import { Metadata, Execution, TestSuites, TestResults } from "./model";
export interface ReportGenerator {
    writeExecution(execution: Execution): void;
    writeMetaData(metadata: Metadata): void;
    writeTestResults(testResults: TestResults): void;
    writeTestSuites(testSuites: TestSuites): void;
}
