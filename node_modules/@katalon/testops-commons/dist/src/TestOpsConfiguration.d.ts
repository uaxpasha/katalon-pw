import { ConfigurationParameters } from "@katalon/testops-api";
import { Metadata } from "./model";
export interface TestOpsConfiguration extends ConfigurationParameters {
    projectId?: number;
    reportFolder?: string;
    metadata?: Metadata;
}
